pull_request_rules:
  - name: Automatic merge on approval
    conditions:
        - and:
          - "#approved-reviews-by>=1"
          - "check-success=Unit tests / build"

    actions:
      merge:
        method: merge
